general {
    colors = true
    interval = 5
}

order += "battery all"
order += "tztime local"

battery all {
    format = "%status %percentage"
    status_chr = "CHR"
    status_bat = "BAT"
    last_full_capacity = true
    integer_battery_capacity = true
}

tztime local {
    format = "%H:%M"
}
